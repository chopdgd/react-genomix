import React from"react";import ReactDOM from"react-dom";import{shallow}from"enzyme";import{TranscriptCell}from"../../index";describe("Test TranscriptCell",function(){it("renders without crashing",function(){var a=document.createElement("div");ReactDOM.render(React.createElement(TranscriptCell,{transcript:"NM_011.1"}),a)}),it("children are rendered correctly (3 children and 1 Link)",function(){var a=shallow(React.createElement(TranscriptCell,{transcript:"NM_011.1"}));// NOTE: space is counted as a child
expect(a.props().children.length).toEqual(3),expect(a.find("Link")).toHaveLength(1)})});